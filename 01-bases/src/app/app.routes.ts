import { Routes } from '@angular/router';
import { CounterPageComponent } from './pager/counter/counter-page.component';

export const routes: Routes = [

    {
        path: '',
        component: CounterPageComponent
    }
];
